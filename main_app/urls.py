from django.urls import path
# from .views import Home, LoginView, VerifyUserView, CoursesView, CreateCourseView, CourseView, CourseStaffView, StudentEnrollmentView, AssignmentsView, CreateAssignmentView, AssignmentView, StudentsView, StudentView

urlpatterns = [
  # path('', Home.as_view(), name='home'),
  # path('users/login/', LoginView.as_view(), name='login'),
  # path('users/token/refresh/', VerifyUserView.as_view(), name='token_refresh'),
  # path('courses/', CoursesView.as_view(), name='courses'),
  # path('course/', CreateCourseView.as_view(), name='create_course'),
  # path('<int:course_id>/', CourseView.as_view(), name='course'),
  # path('<int:course_id>/staff/<int:user_id>/', CourseStaffView.as_view(), name='course_staff'),
  # path('<int:course_id>/student-enrollment/<int:user_id>/', StudentEnrollmentView.as_view(), name='student_enrollment'),
  # path('<int:course_id>/assignments/', AssignmentsView.as_view(), name='assignments'),
  # path('<int:course_id>/assignment/', CreateAssignmentView.as_view(), name='create_assignment'),
  # path('<int:course_id>/assignment/<int:assignment_id>/', AssignmentView.as_view(), name='assignment'),
  # path('<int:course_id>/students/', StudentsView.as_view(), name='students'),
  # path('<int:course_id>/student/<int:student_enrollment_id>/', StudentView.as_view(), name='student'),
  ]